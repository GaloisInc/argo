# Adapted from Stack's "complex configuration" .travis.yml file:
# https://docs.haskellstack.org/en/stable/travis_ci/

# Use new container infrastructure to enable caching
sudo: false

# Do not choose a language; we provide our own build tools.
language: generic

# Caching so the next build will be fast too.
cache:
  directories:
  - $HOME/.ghc
  - $HOME/.cabal
  - $HOME/.stack
  - $HOME/.ghcup
  - $TRAVIS_BUILD_DIR/.stack-work

# The actual build matrix

env:
  global:
  - CABAL_VERSION=2.4.1.0
  matrix:
  - BUILD=cabal GHC_VERSION=8.4.4
  - BUILD=cabal GHC_VERSION=8.6.3
  - BUILD=cabal GHC_VERSION=latest
  - BUILD=stack STACK_RESOLVER=lts-12.26
  - BUILD=stack STACK_RESOLVER=lts-13.11
  - BUILD=stack STACK_RESOLVER=nightly

os:
  - linux
  - osx

# Setting up the build environment

before_install:

# Using compiler above sets CC to an invalid value, so unset it
- unset CC

# Update the submodules (not recursive)
- git submodule update --init

# We want to always allow newer versions of packages when building on GHC "latest" (provided by ghcup, we're not actually using HEAD like the stack version does)
- CABALARGS=""
- if [ "x$GHC_VERSION" = "xlatest" ]; then CABALARGS=--allow-newer; fi

# Download and install the stack executable
- mkdir -p ~/.local/bin
- |
  if [ `uname` = "Darwin" ]
  then
    travis_retry curl --insecure -L https://get.haskellstack.org/stable/osx-x86_64.tar.gz | tar xz --strip-components=1 --include '*/stack' -C ~/.local/bin
  else
    travis_retry curl -L https://get.haskellstack.org/stable/linux-x86_64.tar.gz | tar xz --wildcards --strip-components=1 -C ~/.local/bin '*/stack'
  fi
  # Use the more reliable S3 mirror of Hackage
  mkdir -p $HOME/.cabal
  echo 'remote-repo: hackage.haskell.org:http://hackage.fpcomplete.com/' > $HOME/.cabal/config
  echo 'remote-repo-cache: $HOME/.cabal/packages' >> $HOME/.cabal/config

# Add to PATH: .local/bin (where stack lives), .cabal/bin (where cabal-installed things live)
- export PATH=$HOME/.local/bin:$HOME/.cabal/bin:$PATH

# If using the cabal build type, install cabal and ghc through ghcup
- |
  case "$BUILD" in
    cabal)
      mkdir -p ~/.ghcup/bin
      curl https://raw.githubusercontent.com/haskell/ghcup/master/ghcup > ~/.ghcup/bin/ghcup
      chmod +x ~/.ghcup/bin/ghcup
      export PATH=$HOME/.ghcup/bin:$PATH
      ghcup install-cabal $CABAL_VERSION
      ghcup install $GHC_VERSION
      ghcup set $GHC_VERSION

      # Install build dependencies
      cabal new-install cabal-doctest
      cabal new-install alex
      cabal new-install happy
    ;;
    stack)
      # Install build dependencies
      stack install cabal-doctest
      stack install alex
      stack install happy
    ;;
  esac

install:
- echo "$(ghc --version) [$(ghc --print-project-git-commit-id 2> /dev/null || echo '?')]"
- if [ -f configure.ac ]; then autoreconf -i; fi
- |
  set -ex
  case "$BUILD" in
    stack)
      # Build the dependencies
      stack --no-terminal --install-ghc --resolver $STACK_RESOLVER $ARGS test --bench --only-dependencies
      ;;
    cabal)
      cabal --version
      travis_retry cabal update

      # Determine the list of local packages (using stack)
      PACKAGES=$(stack --install-ghc query locals | grep '^ *path' | sed 's@^ *path:@@')

      cabal new-build --only-dependencies --enable-tests --enable-benchmarks --force-reinstalls --reorder-goals --max-backjumps=-1 $CABALARGS $PACKAGES
      ;;
  esac
  set +ex

script:
- |
  set -ex
  case "$BUILD" in
    stack)
      stack --no-terminal $ARGS test --bench --no-run-benchmarks --haddock --no-haddock-deps
      ;;
    cabal)
      cabal new-build --enable-tests --enable-benchmarks $CABALARGS $PACKAGES
      ;;
  esac
  set +ex

# We handle submodules manually
git:
  submodules: false
